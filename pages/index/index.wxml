
<text class="title">微信小程序demo演示</text>
 <view class="rank-item" wx:for="{{linkList}}" wx:key="{{index}}">
  
  <navigator url="{{item.url}}" class="text">{{item.name}}</navigator>
  <view class='arrow'></view>
</view> 
